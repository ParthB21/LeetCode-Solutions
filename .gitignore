# Secrets — never commit your session cookies
.env

# Note: data/state.json IS committed so the GitHub Action remembers what it
# already synced (it contains no secrets — just submission ids/timestamps).

# Python
__pycache__/
*.pyc
.venv/
venv/
